Keep all work within guidelines and if you feel you need to do work not specified, ask.

When the user adds an issue with category `snag`, Codex should check the current task and decide whether there is a natural opportunity to address it without derailing the active work. If the snag fits the current files or workflow, Codex may work on it in the same session. If not, leave it queued in Codex Companion.

When Codex discovers a non-blocking bug, snag, or future work item, record it with `npm run issue -- add` and include the most specific category available.
